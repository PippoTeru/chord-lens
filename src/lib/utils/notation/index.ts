/**
 * Notation Conversion Module Exports
 *
 * Public API for music notation conversion.
 */

// Degree notation conversion
export { chordToDegree } from './degreeNotation';

// Internal utilities (not exported - used only within notation directory)
// - mNotoConverter (used by chord detection, but could be made public if needed)
